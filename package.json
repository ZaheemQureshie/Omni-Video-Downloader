{
  "name": "video-downloader-root",
  "version": "1.0.0",
  "description": "Root management for Video Downloader project",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && ./.venv/bin/python3 -m pip install -r backend/requirements.txt",
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"cd backend && ../.venv/bin/python3 -m uvicorn main:app --reload\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
